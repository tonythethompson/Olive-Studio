node_modules/
*.diff
.venv/
.venvs/
.venv.building/
.venv.backup-*/
.venv.legacy-*/
.olive-studio/
build/
dist/
*.tgz
src-tauri/target/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Storybook build output
storybook-static/

# Local dev scripts
start-server.bat

# Kiro Crew steering (local agent config)
# Per-developer MCP override — Power config is .kiro/powers/olive-mcp-tools/mcp.json
.kiro/settings/mcp.json

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/
/.freebuff/

# Runtime caches (GenAI model cache, S3 pull staging, accidental ~/  dir)
/.cache/
/~/

# Local agent/tool state
/.ocx/
/.opencode/*
!.opencode/github-commands.md
!.opencode/opencode.json
/.remember/
/.cursor/hooks/state/
/.devin/
/.claude/
/.crush/
.review-threads.json
/dogfood-output/

# Machine-local mcporter imports (paths, env refs, editor copies)
/config/mcporter.json

# Local Olive optimization outputs (ONNX, footprints, run history)
/models/

# Lockfiles for non-pnpm package managers (this repo uses pnpm exclusively)
package-lock.json
yarn.lock
bun.lockb
bun.lock

# IDE / editor config (machine-specific)
.vscode/
.cursor/*
!.cursor/rules/
!.cursor/rules/**
.agents/
.commandcode/
.amazonq/
.idea/

# Agent artifacts (generated, not source)
semantic-review/
skills-lock.json
/.dirac-cache
/.qoder
/.air/
/.playwright-mcp
/.cursor/worktrees
.bobignore
.clineignore
.devinignore
/node_modules.stale-body-guard
install.sh

# Serena (machine-local cache/logs)
/.serena/

# Storybook config (not maintained)
.storybook/
.windsurfrules
QWEN.md
.superdesign/design_iterations/default_ui_darkmode.css
Assets/logo_large.png

# Python caches (hypothesis, ruff)
.hypothesis/
.ruff_cache/

# Olive local optimization run outputs
.olive-runs/

# Tauri auto-generated code (regenerated on build)
src-tauri/gen/

# Husky (legacy, no longer in deps)
.husky/

# Kiro local agent state
.kiro/agents/

# Terraform: live state must never be committed (contains account/region/credentials)
infra/terraform/terraform.tfstate*
infra/terraform/.terraform/
.kiro/specs/cloudflare-env-credential-ux/.config.kiro
/ATXDocumentation
